# Implementation Plans

Generated by the improve skill on 2026-06-29 (audit at commit `44b6f29`).
Findings grouped into three risk tiers — High, Medium, Low — one plan each.
Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

## Execution order & status

| Plan | Title | Risk tier | Priority | Effort | Depends on | Status |
|------|-------|-----------|----------|--------|------------|--------|
| 001  | Make the SQL safety layer real, correct, and tested | HIGH | P1 | M | — | DONE |
| 002  | Source-aware query routing + data/security hardening | MEDIUM | P2 | M | — | DONE |
| 003  | Export escaping fixes + dead-code/default cleanup | LOW | P3 | S | 001 (Step 3 only) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with rationale)

## What each plan covers (audit findings folded in)

- **001 (HIGH)** — finding #2 (no tests/CI on the safety core), #3 (validator
  false-positives on valid `SELECT`s), #1 (UI Run path bypasses validation). Adds
  CI + `validate.rs` tests, removes the substring keyword scan, routes execution
  through `validate_and_run`. **Do this first** — it puts the safety net in place
  before any other change touches query handling.
- **002 (MEDIUM)** — finding #4 (mode mis-routed by keywords, ignoring the data
  source), #6 (API keys in a plaintext file with no permission hardening), #7
  (imported numeric-looking strings lose data). Independent of 001.
- **003 (LOW)** — finding #5 (CSV header / Markdown newline+pipe escaping), #8
  (dead `parse_filters`, duplicated `"llama3.2"` default, removal of the
  now-unused raw `run_query` command). Steps 1-2 are independent; Step 3 needs
  001 merged.

## Dependency notes

- 003 Step 3 (remove `run_query`/`runQuery`) requires 001, which stops the UI
  from using the raw command. 003 Steps 1-2 have no dependency and can run anytime.
- 001 and 002 touch largely disjoint code (001: validator + execution path; 002:
  generation/routing + secrets + import) and can be done in either order, but
  001 is higher priority.

## Findings considered and rejected / deferred

- **Dev-only `npm audit` advisories** (1 high in devDependencies): not runtime-
  reachable (build tooling only); production deps audit clean. Not worth a plan.
- **Unguarded `run_query` command as a security issue**: the connection is always
  read-only and results are capped, so the impact is limited to bypassing the
  advisory checks — addressed indirectly by 001 (routing) + 003 (removal) rather
  than as a standalone security plan.
- **OS-native keyring migration** (`keyring` crate): deferred to a direction/spike
  item, not a fix plan; 002 hardens the existing file instead.
- **Direction items** (first-class MemPalace results UI, pagination beyond 1000
  rows, Postgres/plugin sources): product decisions for the maintainer, not bugs
  — left out of these fix plans by design.
